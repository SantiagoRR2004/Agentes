@startuml
start

:Llamada a moveTowardsAdvanced(Objective);

if (¿Está en la misma habitación que el objetivo?) then (sí)
  :move_towards(Objective);
  if (¿BatteryLevel definido?) then (sí)
    :!reduceBattery;
  endif
  :!reducePatience;
else (no)
  :!goToRoom(ObjectiveRoom);
  note right
    Ver diagrama detallado en
    diagramaGoToRoom.puml
  end note
  :!reducePatience (Reducción adicional);
endif

stop

note right
  El mecanismo de paciencia (reset/exhausted)
  se detalla en diagramaPatience.puml
end note

@enduml